# API Configuration
# Base URLs already include /api path
VITE_API_URL_PRODUCTION=https://your-production-domain.com/api
VITE_API_URL_LOCAL=http://localhost:3000/api

# Environment (development | production)
VITE_NODE_ENV=development

# Note: Make sure your .env file matches these values
# The final endpoint will be: {BASE_URL}/whitelist/register
